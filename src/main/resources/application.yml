server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/meowdb
    username: nekkkkate
    password: meowmeow
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      fail-on-empty-beans: false